import { bookItemInfo, bookLending, catalogData } from './data';

export function getBookLendings(
  data: catalogData,
  memberId: number
): bookLending[] {
  return [];
}

export function addBookItem(data: catalogData, itemInfo: bookItemInfo): void {
  return;
}
